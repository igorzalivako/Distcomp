spring.application.name=orsik

# Не используем БД — только InMemory репозитории (Spring Boot 4)
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration

# Адрес и порт приложения
server.address=localhost
server.port=24110

# JSON: запросы и ответы — плоский формат (без корневой обёртки creator/tweet/label/comment)
